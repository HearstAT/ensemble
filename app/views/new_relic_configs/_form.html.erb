<%= form_for(new_relic_config) do |f| %>
  <% if new_relic_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(new_relic_config.errors.count, "error") %> prohibited this new_relic_config from being saved:</h2>

      <ul>
      <% new_relic_config.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :business_unit_id %>
    <%= f.text_field :business_unit_id %>
  </div>

  <div class="field">
    <%= f.label :account_number %>
    <%= f.text_field :account_number %>
  </div>

  <div class="field">
    <%= f.label :api_key %>
    <%= f.text_field :api_key %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
