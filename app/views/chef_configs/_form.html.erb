<%= form_for(chef_config) do |f| %>
  <% if chef_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chef_config.errors.count, "error") %> prohibited this chef_config from being saved:</h2>

      <ul>
      <% chef_config.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :business_unit_id %>
    <%= f.text_field :business_unit_id %>
  </div>

  <div class="field">
    <%= f.label :chef_server_url %>
    <%= f.text_field :chef_server_url %>
  </div>

  <div class="field">
    <%= f.label :chef_organization %>
    <%= f.text_field :chef_organization %>
  </div>

  <div class="field">
    <%= f.label :admins %>
    <%= f.text_field :admins %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
