<h1>Pager Duty Configs</h1>

<table>
  <thead>
    <tr>
      <th>Business unit</th>
      <th>Sub domain</th>
      <th>Api key</th>
      <th>Service api key</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pager_duty_configs.each do |pager_duty_config| %>
      <tr>
        <td><%= pager_duty_config.business_unit.name %></td>
        <td><%= pager_duty_config.sub_domain %></td>
        <td><%= "*************" %></td>
        <td><%= "*************" %></td>
        <td><%= link_to 'Show', pager_duty_config %></td>
        <td><%= link_to 'Edit', edit_pager_duty_config_path(pager_duty_config) %></td>
        <td><%= link_to 'Destroy', pager_duty_config, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pager Duty Config', new_pager_duty_config_path %>